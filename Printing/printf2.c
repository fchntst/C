#include <stdio.h>

int main()
{
    int x, y;
    float a, b;

     x = 1;
     y = 2;
     a = 2.2;
     b = 3.3;
     printf("Integer numbers: %d + %d = %d", x, y, x + y);
     printf("\nFloat numbers: %f + %f = %f", a, b, a + b);
     return 0;
}